# ========================================
# 🔐 SECURITY BEST PRACTICES
# ========================================
# ⚠️ NEVER commit .env files to version control
# ⚠️ Use .env.local for local development (ignored by git)
# ⚠️ Configure production secrets in your hosting platform (Vercel, etc.)
#
# 🚨 CRITICAL: NEXT_PUBLIC_ prefix exposes variables to the browser
# - ONLY use NEXT_PUBLIC_ for non-sensitive client-side data
# - NEVER use NEXT_PUBLIC_ for API keys, secrets, or credentials
# - Server-side code can access both NEXT_PUBLIC_ and private variables
# - Client-side code can ONLY access NEXT_PUBLIC_ variables
#
# 🔑 Generate secure random keys with:
#   openssl rand -base64 32
#   OR use: https://generate-secret.vercel.app/32
# ========================================

# ========================================
# Supabase Configuration
# ========================================
# Your Supabase project URL (safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Supabase anonymous key (safe to expose - has limited permissions via RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 🔒 Service Role Key (NEVER expose to client - has admin permissions)
# ⚠️ DO NOT use NEXT_PUBLIC_ prefix for this variable
# Use ONLY in server-side code (API routes, server actions)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ========================================
# Application Configuration
# ========================================
# Application URL (safe to expose)
# 🔧 Development: http://localhost:3000
# 🚀 Production: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=development

# ========================================
# Internal API Security
# ========================================
# 🔒 Internal API authentication key (NEVER expose to client)
# Used to secure internal API endpoints
# Generate with: openssl rand -base64 32
INTERNAL_API_KEY=generate_a_random_32_character_string_here

# 🔒 Cron job authentication secret (NEVER expose to client)
# Required for Vercel Cron jobs authentication
# Generate with: openssl rand -base64 32
CRON_SECRET=generate_another_random_32_character_string_here

# ========================================
# AI & External APIs
# ========================================
# 🔒 Google Gemini AI API Key (NEVER expose to client)
# ⚠️ CRITICAL: Changed from NEXT_PUBLIC_GEMINI_API_KEY to GEMINI_API_KEY
# This key must ONLY be used server-side to prevent quota theft
# Get your key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSy...your_gemini_api_key_here

# ========================================
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886